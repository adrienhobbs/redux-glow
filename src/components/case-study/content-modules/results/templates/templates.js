import CalloutBox from './callout-box';
import Standard from './standard';

const ResultsTemplates = (function getResultsTemplates () {
  return {
    CalloutBox: CalloutBox,
    Standard: Standard
  };
}());

export default ResultsTemplates;
